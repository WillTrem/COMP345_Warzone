
#include "TournamentDriver.h"

void testTournament()
{   
    // create a new game engine with the tournament constructor
    GameEngine game(true);
    game.startupPhase();
    
    // Testing the logger function 
    // Tournament* tournament = new Tournament();
    // std::map<int, std::vector<std::string>> gameResults = {{0, {"MAP", "GAME 1", "GAME 2", "GAME 3"}}, {1, {"PLAYER 1", "WIN", "WIN", "WIN"}}, {2, {"PLAYER 2", "LOSS", "WIN", "WIN"}}, {3, {"PLAYER 3", "LOSS", "LOSS", "WIN"}}, {4, {"PLAYER 4", "LOSS", "LOSS", "LOSS"}}};
    // tournament->logResults(gameResults);
}


